import * as React from 'react';
import * as ReactDOM from 'react-dom';
import * as styles from './index.scss';

ReactDOM.render(
    <div>
        <h1 className={ styles.title1_green }>Hello React</h1>
    </div>,
    document.getElementById('root')
);
